all:
	wget --no-check-certificate http://www.cs.toronto.edu/~kriz/cifar-100-binary.tar.gz
	tar -xf cifar-100-binary.tar.gz && rm -f cifar-100-binary.tar.gz

clean:
	rm data_batch*.bin batches.meta.txt readme.html test_batch.bin
